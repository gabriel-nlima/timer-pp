export enum States {
  PAUSED = 'PAUSED',
  STARTED = 'STARTED',
  RESETED = 'RESETED',
  STOPPED = 'STOPPED', // initial
}

export enum StateActions {
  PAUSE = 'PAUSE',
  START = 'START',
  RESET = 'RESET',
  STOP = 'STOP',
}
