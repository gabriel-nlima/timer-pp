export interface TimeObj {
  hours: string
  minutes: string
  seconds: string
}

// espera 0.2 segundo, vibra por 0.3, espera 0.2 segundo e vibra por 0.3
export const VIBRATION_PATTERN_ALERT = [200, 300, 200, 300]
